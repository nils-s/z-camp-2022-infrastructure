# terraform secret variables should never be checked in
secrets.tfvars

# ordinary terraform variables could be checked in, but for now we don't :)
variables.auto.tfvars

# terraform working directory and lock file; will be re-generated by terraform init
.terraform/
.terraform.lock.hcl

# terraform state; should probably be checked in, but can be restored via terraform import; alternatively, a remote backend could be used to keep the state
terraform.tfstate
terraform.tfstate.backup